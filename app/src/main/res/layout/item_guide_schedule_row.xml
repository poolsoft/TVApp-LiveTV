<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/guide_row"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:clipChildren="false"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <!-- Sol Sabit Kanal Hücresi (Zaman cetveli ile tam 240dp hizalı) -->
    <LinearLayout
        android:id="@+id/channel_cell"
        android:layout_width="240dp"
        android:layout_height="58dp"
        android:layout_marginVertical="3dp"
        android:layout_marginEnd="4dp"
        android:background="@drawable/bg_guide_item"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp">

        <ImageView
            android:id="@+id/channel_logo"
            android:layout_width="46dp"
            android:layout_height="32dp"
            android:background="#0D1318"
            android:contentDescription="@null"
            android:padding="2dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_tv" />

        <TextView
            android:id="@+id/channel_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="none"
            android:gravity="end|center_vertical"
            android:maxLines="1"
            android:minWidth="50dp"
            android:paddingEnd="8dp"
            android:singleLine="true"
            android:textColor="@color/accent"
            android:textSize="14sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <TextView
                android:id="@+id/channel_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:singleLine="true"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/current_program"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:singleLine="true"
                android:textColor="@color/text_secondary"
                android:textSize="11sp" />

            <ProgressBar
                android:id="@+id/program_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:layout_marginTop="3dp"
                android:max="100"
                android:progressBackgroundTint="#2E3C4A"
                android:progressTint="@color/accent" />
        </LinearLayout>

        <ImageView
            android:id="@+id/guide_lock_icon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_marginStart="6dp"
            android:contentDescription="@string/locked_channel"
            android:src="@drawable/ic_lock"
            android:visibility="gone" />
    </LinearLayout>

    <View
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="#263E52" />

    <!-- Sağ Program Hücreleri Alanı (Ekranın kalan tüm genişliği) -->
    <LinearLayout
        android:id="@+id/program_cells"
        android:layout_width="0dp"
        android:layout_height="58dp"
        android:layout_marginVertical="3dp"
        android:layout_marginStart="4dp"
        android:layout_weight="1"
        android:clipChildren="false"
        android:gravity="center_vertical"
        android:orientation="horizontal" />
</LinearLayout>
